trigger:
- main

pool:
  vmImage: 'windows-2022'

variables:
  solution: '**/*.sln'
  proyectoMVC: 'ManejoPresupuesto/ManejoPresupuesto.csproj'
  buildPlatform: 'Any CPU'
  buildConfiguration: 'Release'

stages:
- stage: Build
  jobs:
  - job: BuildJob
    steps:
    - task: UseDotNet@2
      displayName: 'Instalar .NET 9'
      inputs:
        packageType: 'sdk'
        version: '9.0.x'

    - task: NuGetToolInstaller@1

    - task: NuGetAuthenticate@1

    - task: NuGetCommand@2
      displayName: 'Restaurar paquetes NuGet'
      inputs:
        restoreSolution: '$(solution)'

    - task: VSBuild@1
      displayName: 'Compilar solucion'
      inputs:
        solution: '**/*.sln'
        configuration: '$(buildConfiguration)'
        msbuildArgs: '/p:DSP=Microsoft.Data.Tools.Schema.Sql.SqlAzureV12DatabaseSchemaProvider'

    - task: CopyFiles@2
      displayName: 'Copiar artefactos de base de datos'
      inputs:
        Contents: '**/*.dacpac'
        flattenFolders: true
        TargetFolder: '$(Build.ArtifactStagingDirectory)/db'

    - task: DotNetCoreCLI@2
      displayName: 'Publicar artefactos web...'
      inputs:
        command: 'publish'
        projects: '$(proyectoMVC)'
        arguments: '--configuration $(buildConfiguration) --output $(Build.ArtifactStagingDirectory)/webapp'
        zipAfterPublish: true

    - task: PublishBuildArtifacts@1
      displayName: 'Publicando artefactos'
      inputs:
        pathToPublish: '$(Build.ArtifactStagingDirectory)'
        artifactName: 'drop'

- stage: Deploy
  dependsOn: Build
  jobs:
  - job: DeployJob
    steps:
    - task: DownloadBuildArtifacts@0
      inputs:
        buildType: 'current'
        downloadType: 'single'
        artifactName: 'drop'
        downloadPath: '$(Pipeline.Workspace)'

    - task: AzureWebApp@1
      displayName: 'Desplegar en Azure App Service'
      inputs:
        azureSubscription: 'ManejoPresupuesto'
        appName: 'ManejoPresupuesto20260316191310'
        package: '$(Pipeline.Workspace)/drop/**/*.zip'

    - task: SqlAzureDacpacDeployment@1
      displayName: 'Desplegar base de datos en Azure SQL'
      inputs:
        azureSubscription: 'ManejoPresupuesto'
        ServerName: 'manejopresupuestodbserver123.database.windows.net'
        DatabaseName: 'ManejoPresupuesto_db'
        SqlUsername: 'ManejoPresupuestoApp'
        SqlPassword: 'tu_password'
        DacpacFile: '$(Pipeline.Workspace)/drop/db/DB.dacpac'
        DeployOptions: '/p:BlockOnPossibleDataLoss=false'